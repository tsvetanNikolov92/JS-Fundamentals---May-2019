function townInfo(townName, population, area) {
    let output = `Town ${townName} has population of ${population} and area ${area} square km.`;
    console.log(output);
}

townInfo('Sofia',
1286383,
492
)